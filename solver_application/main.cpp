#include <iostream>
#include "../formatter_lib/formatter.h"

int main() {
    // Вызываем твою функцию formatter и выводим результат в консоль
    std::cout << formatter("Hello from Solver Application with CPack!") << std::endl;
    return 0;
}
